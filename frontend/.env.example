# Produção na Vercel: deixa vazio ou comenta — usa vercel.json (proxy /api → Railway) e evita CORS.
# Desenvolvimento local: descomenta ou usa o proxy do Vite com URL vazia (vite.config proxy /api → :3000).
# VITE_API_URL=http://localhost:3000
